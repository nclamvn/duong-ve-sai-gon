# Decisions Log — Đường về Sài Gòn (DVSG)

Quyết định của dự án mới, đánh số DV-xxx. Quyết định của engine kế thừa (HT-MB, D-001..D-075) ở `docs/legacy/DECISIONS-HTMB.md` và vẫn có hiệu lực trừ khi bị ghi đè ở đây.

| ID | Ai | Quyết định | Lý do / nguồn |
|----|----|-----------|---------------|
| DV-001 | Chủ nhà | Sau hai demo HT-MB, phát triển nghiêm túc một FPS chiến dịch chơi đơn kiểu CoD, bối cảnh chiến tranh Việt Nam 1971 → 30/4/1975, "thật đến từng cái dép"; cảnh 1 rừng Trường Sơn, vai Quân Giải phóng, AK-47; Claude làm biên kịch | Yêu cầu 2026-09-05 |
| DV-002 | Chủ nhà | 8 nhiệm vụ 1971 → 30/4/1975; một nhân vật xuyên suốt (Nguyễn Đức Thành); cảnh thành phố = Thành cổ Quảng Trị 1972 | AskUserQuestion 2026-09-05 → `docs/story/KICH-BAN-v0.1.md` |
| DV-003 | Chủ nhà | PRD v0.2 (thêm §8 sửa, §19–24: UI/UX, mỹ thuật, điện ảnh, backlog G1) là SOT; ưu tiên chứng minh 15 phút "Cổng Trời" hấp dẫn trước khi mở tám nhiệm vụ | Bàn giao 2026-09-05 |
| DV-004 | Chủ nhà | **APPROVED Blueprint G0′ + G1** (`docs/BLUEPRINT-G1.md`): G0′ 2 tuần (D01–D05), G1 ≈ 8 tuần (D06–D19), G1.5 "M1 hoàn chỉnh" (đêm, AC-130, thám báo, Thu) là gate riêng trước G2 | 2026-09-05 |
| DV-005 | Thầu (Chủ nhà chấp thuận qua APPROVED) | Điện ảnh: cutscene **trong engine** (camera track JSON, cùng asset gameplay) là chính + công cụ ghi MP4 1080p24 cho CINE-00/briefing; Blender chỉ dự phòng nếu animatic D16 không đạt | Blueprint G1 R3 |
| DV-006 | Thầu (Chủ nhà chấp thuận) | Fork `hai-tuyen` (8745ba3) → repo `duong-ve-sai-gon`; engine + pipeline + test giữ nguyên; content Hải Tuyến (arena, Phố Vạn Hải, mission G0) giữ làm **test/bench** (`?level=arena` mặc định, `?level=pho`), không phải content game; docs HT-MB vào `docs/legacy/` | TIP-D01 |
| DV-007 | Thầu | Khẩu mẫu G1 = AK-47/Type 56 báng gỗ 1971 (không dùng AK-74M của HT-MB) | Blueprint G1 §1 |
| DV-008 | Thầu | `frame_p95.target_g1 = 16.67 ms` thêm vào budget (ADR-D02); ngưỡng đỏ giữ; không sửa để "đạt" | PRD v0.2 §22.3 |
| DV-009 | Thầu | Mọi level mới bắt buộc E2E vật lý người chơi trong CI (kế thừa D-075); mọi asset lịch sử phải có `registryId` trỏ registry có provenance P/S; asset ngoài phải qua kiểm rip (D-072) | PRD v0.2 §7.2, §12 |
| DV-010 | Thầu | Cutscene/video không chặn tiến trình mission (mission không phụ thuộc video chạy hết); người thật có tên không có lời thoại bịa; không nhân bản giọng người thật | PRD v0.2 §22.2, §9.2, AUD-005 |
| DV-011 | Thợ (L1) | Tìm thấy WIP TIP-023 chưa commit trong sandbox HT-MB (spline + setpiece technical/flyby, typecheck + unit xanh, chưa render/E2E) → cất ở nhánh `tip-023-wip` của `hai-tuyen`, không đưa vào fork; dùng lại cho `engine/vehicles` ở G2 sau khi nghiệm thu | TIP-D01 |
| DV-012 | Thợ (L1, ADR-D04) | Encoder KTX2 = `ktx2-encoder@0.6.0` (WASM basis_universal, npm thuần) thay `toktx`/KTX-Software (sandbox không tải được binary; npm thuần → Mac không cần brew, pipeline tái lập). Quy tắc: diffuse/baseColor ETC1S q160 sRGB · arm/ORM ETC1S q128 linear · normal UASTC (normal preset, RDO λ1, zstd) · mipmap sinh lúc mã hoá. 110 file, −34,1 MB, manifest 113,4 MB, JPG xoá khỏi `public/` (nguồn `assets-src/`) | TIP-D02, `evidence/TIP-D02/ktx2.txt` |
| DV-013 | Thầu | Budget `initial_payload_mb` 150/250 → **250/400**, `level_total_mb` 750/1200 → **2500/4000** — theo PRD v0.2 §11 (bảng ngân sách, đã duyệt DV-003), có ADR-D04 bổ sung; không phải nới để "đạt" (manifest hiện 113 MB, dưới cả mốc cũ) | ADR-D04 addendum |
| DV-014 | Thợ (L1) | Trả nợ perf HT-MB TIP-026: biển hiệu gộp atlas (2×10 ô 1024×192 / trang 2048×1920, ≤ 1 mesh/trang, không đổ bóng) + InstancedMesh prop có bounding sphere và `frustumCulled = true`. Draw `?level=pho` −184 trung bình (−27 %), 4/5 góc đạt AC ≥ 150, góc b −146 → **deferred mức thấp**, xử lý cùng LOD/impostor ở D05/D08; tam giác 2,1–3,25 M (3/5 góc vượt target 2,5 M, 0 vượt đỏ) ghi nợ D05 | `evidence/TIP-D02/draws-before-after.txt` |
| DV-015 | Thợ (L1) | Validator asset (`scripts/validate-assets.mjs`, chạy trong unit test + `npm run assets:validate`): allow-list license thêm `PD-USGov` (dữ liệu chính phủ Mỹ — SRTM); manifest schema thêm type `terrain`, source `aws-terrain-tiles`; texture rời bắt buộc `.ktx2`, GLB không còn `image/*` ngoài ktx2; tam giác theo tiền tố (chr_ 25k, wpn_ 20k, veh_ 40k, air_ 15k, veg_ 8k, prop_ 3k) là WARN (asset kế thừa không chặn), `--strict-tris` để FAIL | TIP-D02 |
| DV-016 | Thợ (đề xuất, chờ Chủ nhà chốt toạ độ) | Nguồn DEM cho terrain M1: **SRTM 1″ NASA/USGS (public domain) qua Terrain Tiles on AWS** (`skadi/N17/N17E106.hgt.gz`, sandbox tải được). Ô thử `truong-son-a`: tâm 17,55°N 106,10°E, 2048 m @ 2 m (1025²), z 522–892 m; chi tiết < 30 m là fBm seeded và ghi rõ "procedural, không phải đo đạc" trong `meta.json`. Toạ độ ô chính thức chốt ở TIP-D04 sau khi Chủ nhà chọn khu (đường 20 Quyết Thắng / ATP) | `scripts/terrain-bake.mjs`, `public/assets/terrain/truong-son-a/` |
| DV-017 | Thợ (L1) | Đường transcoder KTX2 = `${BASE_URL}basis/` (không phải `${BASE_URL}assets/basis/`): lỗi boot "Unexpected identifier 'html'" vì Vite trả `index.html` cho đường sai → thêm probe boot vào quy trình (arena + pho) trước khi đo | TIP-D02 |
| DV-018 | Thầu | **ADR-D05 terrain** (TIP-D04): ô 64 m × 4 LOD instanced (≤ 4 draw/2 km), cao độ từ texture RGBA8 trong vertex, váy 6 m; splat 4 lớp theo quy tắc slope/height (không splat map) + tint lớp về tông nhiệt đới; một heightfield Rapier cho cả ô; navmesh bake offline `nav.bin` (fallback bake runtime); level terrain schema riêng (`terrain-level.schema.json`), `?level=truong-son`. Chưa streaming/CDLOD — D08 quyết | `docs/ADR/ADR-D05-terrain.md` |
| DV-019 | Thợ (L1) | `fetch-assets.mjs` giữ nguyên entry Poly Haven đã có trong manifest (đã KTX2), chỉ tải id mới hoặc `--force id`; trước đây chạy lại sẽ ghi đè `.ktx2` bằng `.jpg` | TIP-D04 |
| DV-020 | Thợ (L1) | Vite trả `index.html` (200, text/html) cho file thiếu → mọi fetch file nhị phân tuỳ chọn (`nav.bin`) phải kiểm `content-type` + byte đầu, và navmesh nạp có 0 poly → bake runtime (không tin `r.ok`) | TIP-D04, lỗi gặp khi probe |
| DV-021 | Thợ (L1) | Terrain: roughness sàn 0,92 trộn 30 % ARM (ARM Poly Haven 0,5–0,7 làm mặt đất phản chiếu trời ở góc xiên → nhợt bạc); bóng nhận qua `receivedShadowPositionNode` = vị trí + normal terrain × 0,35 m (geometry instanced không có normal per-vertex → normalBias mặc định vô hiệu → acne vạch ngang trên sườn) | TIP-D04 evidence |
| DV-022 | Thợ (L1, báo Chủ thầu) | GTAO mờ dần theo khoảng cách nhìn 60 → 140 m (`post.ts`, mọi level): thí nghiệm `post=low` sạch / `shadow=0` vẫn vạch → GTAO nửa phân giải trên mặt terrain xa tạo ô theo tam giác LOD + vạch ngang; AO > 60 m không nhìn thấy nên không đổi cảm nhận pho/arena (Chủ nhà kiểm trên Mac nếu nghi). Camera `near` 0,25 riêng level terrain (depth tới 2,6 km) | `evidence/TIP-D04/completion-report.md` |
| DV-023 | Thợ (L1, luật mới) | **Ngân sách binding WebGPU:** ≤ 16 texture và ≤ 16 sampler mỗi shader stage (Apple M-series: adapter 48 texture nhưng sampler cứng 16; three tạo 1 sampler/texture). Vật liệu terrain lúc đầu 15 texture_2d + 3 depth CSM = 18 → pipeline WebGPU fail **im lặng** (draw đếm, không vẽ; console không lỗi) → Mac chỉ thấy trời. Sửa: lớp đất dùng normal mùn, cỏ dùng normal+arm mùn, đá arm hằng → 11 + 3 = 14 texture, 13 sampler. Luật: mọi vật liệu mới phải tính binding = texture lớp + 3 CSM + PMREM + DFG LUT ≤ 16; kiểm trên Mac WebGPU (sandbox chỉ có WebGL2, giới hạn 32) trước khi nghiệm thu; lớp thứ 5+ của terrain phải dùng texture array (D12) | Chẩn đoán qua Chrome của Chủ nhà (`renderer.debug.getShaderAsync`, `device.limits`) |
| DV-024 | Thợ (L1) | Post: sàn SSR (`ssrWetFloor` 0,22) chỉ áp cho mặt nhẵn/ướt — nhân `smoothstep(0,6 → 0,35, roughness)`; mặt nhám (terrain 0,8–0,9, tường) không còn ô/vệt SSR nửa phân giải trên WebGPU. GTAO fade 60→140 m siết thành **30→70 m** (Mac WebGPU: `post=low` sạch, `post=medium` có vạch từ ~50 m; AO bán kính 0,5 m ở > 30 m không nhìn thấy). Pho/arena: sàn ướt (roughness 0,1–0,3) giữ SSR; góc nhà > 30 m mất AO — chấp nhận, Chủ nhà kiểm cảm nhận | Ảnh `evidence/TIP-D04/mac-webgpu-spawn.jpg` |
| DV-025 | Chủ nhà | Sau khi xem ảnh Mac D04: "sai hết so với PRD về trang phục lính VN trước 1975 (rằn ri nước ngoài), tay trái không ôm súng, rừng không cây → cần rất nhiều asset hình ảnh". Đổi thứ tự G0′ thành **asset sprint**: D10 AK-47/Type 56 → D11a tay FP + thân 1971 (mũ cối, dép, bao xe **procedural** trước, CC-BY sau nếu cố vấn yêu cầu) → D05 rừng **loài thật** ngay từ đầu (gộp phần asset của D12), vẫn giữ gate perf rừng ≤ 12 ms GPU. Mỗi TIP có ảnh Mac WebGPU để Chủ nhà chấm; fact quân phục tier H → `approved: false` tới khi duyệt registry + cố vấn | AskUserQuestion 2026-09-05 |
| DV-026 | Thợ (kiểm nguồn) | Loại ứng viên Sketchfab do nhiễm license: "Banana Tree" (Garecra) dùng texture Quixel Bridge (Megascans chỉ cho UE, không tái phân phối CC-BY); "Jungle Tree" (kobaltsecond) kitbash freebie người khác không rõ license; "Realistic Jungle Tree" Garecra 0,7–2,3 M tri (nanite) ngoài ngân sách. Từ chối AK "jeandiz"/"billyjackman3d" (tag Squad/CS/Battlefield). Mọi model tải phải có mô tả gốc rõ tác giả tự làm | sfq.py + API `/v3/models/{uid}` |
| DV-027 | Thợ (L1, D11a) | Mixamo Swat Guy **không có sọ và bàn chân riêng** (vỏ mũ = đầu, mặt balaclava tới mũi, giày = gear) → D11a giữ vỏ mũ sơn da làm sọ + mũ cối procedural trùm ngoài (chỏm 0,30 × 0,34 m), giày giữ sơn đen; dép cao su/đầu thật/ba lô/bi đông → **D11b** cần mesh mới (CC0/CC-BY hoặc đặt). Gear 1002 bỏ 80 % (32 841 → 6 502 tam giác); atlas sơn lại theo island (chuẩn hoá sáng/tối từng island để giáp đen và rằn ri cùng về một tông vải) | `scripts/retexture-1971.mjs`, ảnh `evidence/TIP-D11a/` |
| DV-028 | Thợ (L1) | Asset nhân vật 1971 = **dẫn xuất Mixamo** (license Mixamo cho phép dùng trong game, không tái phân phối mesh gốc — `assets-src/` không commit) + vải Poly Haven CC0; manifest `soldier_mixamo` chuyển `historical: true`, `registryId uni.pavn.1971.field_uniform_green` (tier H) → `approved: false` cho tới khi cố vấn có ảnh/hiện vật (DV-009). Bao xe theo `chest_rig_type56` (H), mũ cối theo `helmet_pith` (S) | registry lượt 1 |
| DV-029 | Thợ (L1, sau ảnh Mac Chủ nhà) | Pose tay FP của AK-47 **không** lấy từ `fit()` tự động trên clip Mixamo (tay trái raw lệch 8 cm → cổ tay gập, "cánh tay như bị tật") mà dùng pose AK-74M đã kiểm trên Mac (TIP-017) vì cùng hệ anchor (gripL z −0,13; gripR lệch 1,5 cm) → measure R 4,9 mm / L 8,3 mm. Luật: pose tay FP là số đã kiểm bằng mắt trên Mac; `fit()` chỉ là điểm xuất phát, phải qua `?calib=fp` chấm trước khi ghi JSON. Đai cổ tay găng Swat sơn thành cổ tay áo (không phải da) để che khe ống tay–bàn tay | `evidence/TIP-D11a/mac-fp-hip-before-fix.png`, `fp-before-after.jpg` |
| DV-030 | Thợ (L1, sau ảnh Mac lần 2) | Anchor `gripL` phải là **tâm ốp lót đo từ silhouette** (`scripts/weapon-silhouette.mjs`, áp world matrix vì đỉnh đã quantize), không lấy từ bảng profile z-slice (AK-47 CRWDE: ốp lót z −0,23..−0,12 nhưng gripL −0,13 rơi vào đầu hộp khoá nòng → ngón cuộn qua thép). Kiểm ôm/xuyên bằng **toạ độ khớp ngón trong hệ anchor** (Index1/2/4, Middle4, Thumb4 so hộp ốp lót ±1,75 × 3,8 cm) chứ không bằng `measure().error` (chỉ là trọng tâm ngón ↔ anchor). Ngón cái trái khép quanh −z local (đo 6 trục) — `THUMB_L_CLOSE` trong `fpArms.ts`; hip.pos AK-47 gần camera thêm 2 cm vì tầm với tay FP 0,5 m | `evidence/TIP-D10/measure.txt`, `silhouette-ak47-ak74m.png` |
| DV-031 | Thợ (L1, sau ảnh Mac `?level=truong-son`) | Bot **bám mặt đất mỗi tick di chuyển** (`Bot.settleY`): terrain heightfield (`deps.groundHeight` = `terrain.heightAt`) hoặc `nav.nearest` (< 1,5 m ngang), fallback y góc kế. Trước đây `position.y = wp.y` (y của góc kế trong straight path) — trên arena phẳng vô hại, trên terrain đoạn thẳng 64 m lệch 16 m → "quân địch bay trên không trung" (Chủ nhà). Unit `ai.test` mặt dốc 20 % (trước sửa lệch 15 m), E2E `level-truong-son` kiểm bot gap < 0,5 m | Probe sandbox `__ht.bots()` vs `terrainHeight` |

| DV-032 | Thợ (L1, D05) | Rừng dùng **InstancedBufferGeometry + attribute instance riêng** (`iPosScale` vec4, `iRotTint` vec4 = cos/sin yaw, tint, pha gió) mỗi (loài × biến thể × LOD × material), không BatchedMesh (backend WebGPU r185 phát một drawIndexed mỗi instance) và không InstancedMesh (64 B ma trận/instance). Geometry loài phải **nướng ma trận node** (quantize KHR đặt POSITION int16 + node T/S) vì `positionGeometry` đọc thẳng attribute — quên là cây to 2 m/đặt sai; bbox tính sau khi nướng (impostor `sizeM`). LOD theo ô 64 m, ô vắt ngưỡng → LOD từng cây; hysteresis chỉ khi đổi đúng một bậc (đổi nhiều bậc khi teleport từng "giữ" LOD0 cho ô 120 m) | `src/engine/vegetation/system.ts`, `species.ts` |
| DV-033 | Thợ (L1, D05) | Impostor 8 phương vị bake lúc nạp: viewport/scissor mỗi ô phải đặt qua **`rt.viewport`/`rt.scissor`** (Renderer WebGPU lấy từ render target, `setViewport` chỉ cho canvas → cả atlas là một ảnh kéo giãn); alphaTest khi bake 0,2 (ô 256 px ≈ 10 cm/px lấy mip thô của lá, alpha trung bình < 0,5 xoá gần hết tán); atlas linear (không sRGB) + AO dọc 0,55→1 × 0,85 để khớp cây LOD2 kề bên. Octahedral (PRD VEG-001) để D12 | `src/engine/vegetation/impostor.ts`, `evidence/TIP-D05/atlas-tree_gn.png` |
| DV-034 | Thợ (L1, D05) | LOD lá dạng thẻ = tỉa thành phần liên thông giữ tỉ lệ, phóng thẻ còn lại 1/√keep **≤ 2,2** (trước 1,6: LOD2 giữ 12 % → độ phủ 31 %, cây "trụi lá" ở 60–170 m; nay tree LOD [1, 0,35, 0,15] → phủ ≈ 1,0/0,73); tầng thấp LOD lean (fern [0,7, 0,25, 0,08], ráy [0,6, 0,25, 0,08]) và ngưỡng ngắn (fern 12/28/70 m, ráy 18/40/90 m) vì cây 1 m ở 90 m là 7 px. GLB thực vật **không normal map** ở D05 (material TSL không dùng; −3,3 MB KTX2). Ngân sách validator `veg_` 8 k → 22 k (LOD0 cây tán, chỉ hiện < 32 m) | `scripts/convert-vegetation.mjs`, `content/vegetation/species.json` |
| DV-035 | Thợ (L1) | `TerrainTile.minY/maxY` tính từ giá trị **float32 đã lưu** (trước tính từ double → `sample()` trả float32 nhỏ hơn `minY` vài ε → mặt nạ cao độ loại cây ở đáy ô; ô phẳng tổng hợp cho 0 cây). `renderer.info` reset theo tick rAF: E2E đo draw/tris phải render mỗi mẫu trong một tick rAF riêng (nhiều `frame()` trong một task cộng dồn 237 → 683 calls) | `src/engine/terrain/tile.ts`, `tests/e2e/level-truong-son.spec.ts` |
| DV-036 | Thầu (đề xuất, chờ Chủ nhà bench) | **ADR-D06 rừng** (= "ADR-D01 Rừng" của Blueprint): kiến trúc D05 giữ cho G1 nếu bench Mac GPU rừng ≤ 12 ms (`?level=truong-son&bench=1` so `?veg=0`, 3 run); ADJUST theo thứ tự: bóng chỉ LOD0/cascade gần → impostor sớm (170 → 130 m) → atlas 1 material/loài (draw 70 → ~30, đạt PRD ≤ 40) → bỏ LOD0 tầng thấp ngoài 10 m → mật độ high 0,8. Sandbox high: rừng 1,1–1,25 M tam giác, 62–78 draw, tổng renderer 2,3–3,05 M (bóng LOD0+1 × 3 cascade ≈ 1–1,8 M), CPU 0,5–0,7 ms. Đè cỏ/subsurface/compute LOD/octahedral/ground cover → D12; navmesh obstacle cây → D08 | `docs/ADR/ADR-D06-rung.md` |
| DV-037 | Thợ (L1, sau ảnh Mac D05 lần 1) | Chủ nhà: "gốc cây trồi", "không có cỏ… cần dày, nhiều tầng, nhóm cây có dây leo". (a) Luật `sink: [m, × tan dốc]` mỗi loài — rễ bạnh Tree GN r ≈ 3 m nên trên sườn 20 % mép rễ hở 0,6 m; chôn 0,35 + 2·tanθ (tre 0,15 + 1,2·tanθ). (b) **Cỏ procedural** `gen-grass.mjs` (texture lá cỏ vẽ bằng code, thẻ chéo 3 quad × 3 đoạn, 3 biến thể × 3 LOD 16/8/2 tam giác; manifest `source: procedural`, CC0) 22 000/ha trong 640 m quanh vùng chơi (714 k instance, cull 42 m) + **đè cỏ** VEG-006 qua uniform `press` (chân người chơi, r 0,9 m; `pressM` theo chiều cao loài); thêm monstera (dây leo lá xẻ, `noiseId: tree_gn` → mọc trong cụm cây tán) và fern_grass (POLYSCAN) từ nguồn đã tải; cây tán 40/ha. Sức chứa batch = ước lượng đĩa lod[3] × 1,6 (không cấp phát cả rect). Draw rừng 99–117 (9 loài) — atlas D12 | `evidence/TIP-D05/fix1-*`, completion report "Vòng 1" |
| DV-038 | Thợ (L1, sau ảnh Mac D05 lần 1 — tay) | Tay trái FP phải **ôm ốp lót từ dưới** (lòng bàn tay lên, ngón vắt qua mặt phải-trên, ngón cái mép trái-trên) — pose kế thừa AK-74M đặt tay bên trái ốp. Rot tính **giải tích** từ trục local bàn tay Mixamo (đo ở rot = 0: +y hướng ngón, +z lòng bàn tay, +x trỏ→út) → Euler XYZ (−1,571, −0,418, −1,571); ngón co riêng trái `FINGER_CURL_L` (đốt 3 co 1,45 rad — đầu ngón đè mặt trên), ngón cái lưới 18 tổ hợp (curl [0,25, 0,35, 0,25], khép [0,5, 0,5]), chỉnh runtime `game.fpArms.thumbL`. **Tầm với tay FP 0,498 m** (vai (−0,19, −0,19, +0,07) hệ camera): ốp lót AK ở 0,44 m trước camera là ngoài tầm 6,5 cm → IK kéo cổ tay lùi, ngón xuyên — `hip.pos` z −0,28 → −0,215, x 0,11 → 0,085 (ốp 0,38 m; ADS không đổi vì alignSight → 0,305 m). Luật: mọi khẩu mới kiểm `fpArms.debugInfo().L.dist ≤ reach` trước khi chấm pose | `content/weapons/ak47.json`, `src/engine/render/fpArms.ts`, `evidence/TIP-D05/fix1-sandbox-fp-*` |
| DV-039 | Thầu (đề xuất) + Thợ (L1 phần khói) | Chủ nhà: "cần bổ sung khói lửa, máy bay… tiêm kích, trực thăng và lính dù". Làm ngay: `fx` cho level terrain (schema, `loadTerrainLevel` → `createAmbientFx` HT-MB; `FxDef.height` cột khói m) — 3 cột khói bom 300–420 m ở 500–700 m quanh tuyến + đám cháy gần tuyến. **Máy bay = TIP mới "D-SKY"** sau D08 (spline bay seeded, âm Doppler D09, gió rừng theo trực thăng — VEG-002): cần Chủ nhà tải Sketchfab CC-BY đã kiểm mô tả (UH-1B TonyWony 13 k; F-4 andertan 62,7 k + ETAN798 8,3 k; A-1 Rhine_Lab 58 k; C-130 Tyler_Dave 25,5 k); Mi-24 HT-MB không dùng (không có ở 1971). **Lính dù: chờ cố vấn** — 1971 không có nhảy dù xuống Trường Sơn; Sư đoàn Dù VNCH/Lam Sơn 719 và SOG đổ bằng trực thăng → thay bằng đổ quân UH-1 hoặc dù phi công máy bay trúng đạn; registry `evt.1971.lam_son_719` (DV-009/010) | Completion report TIP-D05 "Vòng 1" §4, SUGGESTIONS |
| DV-040 | Thợ (L1, D05 vòng 2) | **Bot không đi xuyên cây:** navmesh bake `terrain-bake.mjs --nav --level <level.json>` chạy scatter của engine trong Node (bundle rolldown từ `scatter.ts`/`tile.ts`/`navObstacles.ts`, density 1) → mỗi collider thân = lăng trụ 8 cạnh vào lưới đầu vào recast; runtime bake (không có nav.bin) thêm cùng lăng trụ từ `forest.system.colliders`. Hai bài học: (1) **nắp lăng trụ phải ở 1,2 m** — nắp 0,6 m ≤ walkableClimb (0,35 m → 2 cell 0,5 m) bị recast coi là **bậc thang leo được** (`filterLowHangingWalkableObstacles`) → không carve; (2) đáy/nắp tính từ **mặt navmesh** (`TerrainTile.sampleGrid` lưới 4 m), không phải terrain thật hay đáy cylinder đã chôn (`sink`) — lệch 1–2 m trên sườn làm lăng trụ chui xuống đất/bay lên. `density` tier = **tập con** của placement đầy đủ (giữ điểm lưới hash(jitter) < density) thay vì đổi bước lưới → cây tier thấp ⊂ tier cao → nav.bin bake density 1 đúng ở mọi tier. nav.bin 303 → 741 KB (1 366 obstacle, 4 554 poly); E2E: 60 thân trong navRect không có điểm navmesh | `scripts/terrain-bake.mjs`, `src/engine/vegetation/navObstacles.ts`, `tests/e2e/level-truong-son.spec.ts` |
| DV-041 | Thầu (TIP-D-SKY) + Thợ | **Bầu trời = hệ lượt bay seeded** (`engine/sky`): FlightDef (model/kind/đội hình/AGL trên max địa hình dọc đường/speed/period/hover/parachute/gust/spin) → lịch PRNG, đường thẳng qua vùng chơi dài 2R, treo giảm tốc, rotor quay, bóng CSM; gió xoáy trực thăng → `forest.setWind` (VEG-002); âm procedural 3D Doppler (`AudioEngine.aircraft`); dù phi công procedural thả ≥ 400 m. Level `airTraffic` (F-4 cặp, F-4 trúng đạn → dù, UH-1 cặp, UH-1 treo đổ quân (−560, 240), A-1, C-130). **Lính dù**: 1971 không nhảy dù xuống Trường Sơn (Lam Sơn 719/SOG đổ bằng trực thăng) → dù chỉ là phi công; chờ cố vấn registry `evt.1971.lam_son_719`. Model air_* cần Chủ nhà đăng nhập Sketchfab (CI dùng stand-in `?skyModel=veh_mi24`; Mi-24 không vào level) | `docs/tips/TIP-D-SKY.md`, `contracts/TIP-D-SKY.yaml` |
| DV-042 | Thợ (L1, D-SKY vòng 2 — asset thật) | **Tải Sketchfab qua tài khoản Chủ nhà đăng nhập trong Browser pane** (không nhập mật khẩu): `fetch('/i/models/<uid>/download')` liệt kê archive → `fetch('/i/archives/latest?archiveType=glb&model=<uid>&textureMaxResolution=…', {credentials:'include'})` trả URL S3 ký (~6 phút) → curl về `assets-src/sketchfab/` (gitignore). **Kiểm nguồn trước khi dùng** (D-072/DV-026): loại F-4 andertan (texture `Eurofighter_RT`, `F15_R` — ghép từ model khác), A-1 Rhine_Lab (node `AH-1J_node…` kitbash), 42manako F-4E ("Model by PAV" — người tải không phải tác giả), manilov.ap (hàng chục máy bay mô tả Wikipedia, không rõ nguồn), bohmerang/thomas333 B-52 trùng số mặt → lấy bohmerang (thomas333 tự ghi "credits to Bohmerang"), C-130 Tyler_Dave không có archive tải. **Dùng**: UH-1B TonyWony (13 k, rig joint), F-4 luacha2000 (1,5 k, "asset I made for a game", sơn SEA), B-52 bohmerang (16,6 k → simplify 0,85 = 14,4 k, ngân sách air_ 15 k), AC-130 helijah (tác giả FlightGear; 176 k có buồng lái đầy đủ → `--keep` 12 node vỏ ngoài + cánh quạt, `--simplify 0,28` = 12,7 k, 3 draw; bỏ pháo → C-130 vận tải). ETAN798 F-4 sạch nhưng pose nghiêng + bom rơi trong cùng mesh → bỏ. **convert-model**: thêm `--pre x:90` (xoay trước khi căn trục) và `--split-joints rotor_01,tail_rotor_02:z` (tam giác thuộc joint theo trọng số → primitive riêng dưới node pivot tại gốc joint; trục khác y bọc node khung `frame_*` không khớp regex spin) → rotor UH-1 quay thật; F-4/B-52 mũi ở −x → `--flip`; **UH-1B KHÔNG flip** (lần đầu flip nhầm vì ảnh kiểm chụp từ bản dist cũ → Chủ nhà thấy "trực thăng bay giật lùi"; sửa cùng ngày). Luật mới: kiểm chiều mũi bằng **hình học** (tâm primitive buồng lái/kính/cánh quạt phải ở +x — `Object_8 cockpit tâm +2,5`) chứ không bằng ảnh; E2E bầu trời kiểm bộ phận mũi đi trước tâm thân theo vận tốc (`NOSE_MAT` mỗi model). B-52/C-130 sải cánh > dài → `--yaw90`. **Level**: A-1 tạm bỏ (chưa có model sạch); C-130 450–650 m; thêm tổ 3 B-52 1 100–1 400 m (AGL mọi lượt hạ ~35 % theo Chủ nhà "quá cao") (thực tế ~9 km, nén để còn thấy — cố vấn duyệt); điểm treo đổ quân dời (−560, 240) → **(−624, 480)** = bãi trống tự nhiên (`scripts/find-clearing.mjs`: không cây thân trong 28 m, dốc 14°) vì điểm cũ nằm trong tán cây → không thấy trực thăng. E2E dùng model thật (bỏ stand-in), thêm kiểm rotor quay | `evidence/TIP-D05/sky-air-*.jpg`, `sky-f4-pair.jpg`, `sky-hover-clearing.jpg`, `sky-b52-cell.jpg`; `scripts/convert-model.mjs` |
| DV-043 | Chủ nhà (AskUserQuestion 2026-09-06) + Thầu | Sau ảnh Mac `83ce72b` Chủ nhà đòi **nâng cấp triệt để, không quick fix** (tay như cục thịt, trực thăng bay giật lùi/quá cao, không bản đồ/mục tiêu, không đồng đội/địch, chữ HUD thô). Blueprint §8 **G1-A** chạy trước D06–D09; Chủ nhà chốt vòng 1 = **trận đánh + HUD** (TIP-M1A + TIP-UX02), bản đồ = **la bàn + marker 3D + minimap + bản đồ chiến thuật** (sửa PRD §8.1: minimap bật mặc định M1, tắt bằng N), tay = **Sketchfab CC-BY rigged** (TIP-D11b sau). Thợ: (a) HUD DOM/CSS mới, font OFL tự host (Barlow Condensed số, Be Vietnam Pro chữ — manifest type `font`, license `OFL-1.1`), la bàn bắc = −z, marker chiếu `camera.project` + kẹp mép, minimap canvas xoay theo hướng nhìn từ **bản đồ giấy bake** (`terrain-bake --map`: hillshade + đồng mức 20/100 m + tông rừng từ scatter), địch trên bản đồ chỉ khi vừa bắn ≤ 3 s (công bằng §20.3); hit marker kín đáo (D10 nói "không arcade" — làm nhỏ 0,28 s, tắt được ở D06); HIT có `shooter` → hit marker/vòng hướng. (b) Mission M1 data-driven: action mới `sky_trigger`/`squad_order`/`objective.marker`, condition `active_ms` (tính từ lúc action chạy — `timeout` đếm cả thời gian chờ zone nên n_ridge thoát ngay), spawnGroup `archetype recon/squad`, `faction`, `spawns[]`, `names[]`; **phe** trong Bot qua deps (`target()` = địch gần nhất khác phe, `hitMask`, `followGoal`), cùng phe không sát thương, người chơi không sát thương đồng đội (tạm); đồng đội = `Squadmates` (Quyết dẫn 8 m/chờ khi ta tụt > 14 m/đuổi khi ta vượt; Hải sau 3 m lệch 2 m; hold), bark cue ưu tiên thấp chống spam; thám báo = **shader rằn ri hổ** trên atlas QGP (mặt nạ vải theo sắc độ, không texture mới — `soldier-swat.glb` nguồn không còn ở sandbox/Mac → D11c dựng lại từ `swat.fbx`); tuyến tuần tra địch = cover marker quanh spawn; cover marker sinh sau gốc cây (script); **LZ dời (−624, 480) → (−292, 306)** vì điểm cũ ngoài navRect (bot không tới được), `huey_insert` chỉ mission kích hoạt | `docs/BLUEPRINT-G1.md` §8, `docs/tips/TIP-UX02.md`, `docs/tips/TIP-M1A.md`, `evidence/TIP-UX02/*`, `evidence/TIP-M1A/*` |
| DV-044 | Chủ nhà (ảnh Mac 2026-09-06: "vẫn tồn tại bảng thông số đã yêu cầu bỏ") | **Telemetry overlay không hiện mặc định** ở mọi build (trước: dev luôn hiện) — chỉ `?overlay=1`, bench, hoặc F3 lúc chơi. **Level mặc định = `truong-son` (M1)** khi không có `?level` (trước: arena bench Hải Tuyến → Chủ nhà mở link thiếu tham số thấy "Tiến vào khu relay phụ"); arena/pho chỉ qua `?level=`; E2E/bench vẫn ghi rõ `level=arena`. Nút màn capability đổi nhãn theo level | `src/main.ts`, `src/game/game.ts`, `src/ui/capability.ts` |
| DV-045 | Thợ (L1, sau ảnh Mac 2026-09-06: "bị bắn gục không ngã hẳn, đứng nghiêng nghiêng"; "đồng đội chạy xuyên qua cây") | **(a) Chết nằm hẳn:** gốc lỗi ở `CharacterInstance` — one-shot `hit` (2,29 s) `clampWhenFinished` giữ weight 1 ở frame cuối, và khi `death` phát thì clip đang chạy không bị fade → mixer trộn 50/50 hit-đứng + death → "đứng nghiêng" (đầu 0,92 m). Sửa: `playOnce` fade one-shot cũ, `death` fade mọi action đang **scheduled** (kể cả paused/clamp — `isRunning()` bỏ sót), listener `finished` fade clip vừa xong (trước: đông cứng weight 1 mãi → mọi bot sau phát trúng đạn đầu tiên đều pose lệch). E2E glTF ở `boot.spec` (arena có soldier.glb): sau chết 3 s chỉ `death_front` weight 1, đầu < 0,9 m; đã xác nhận fail trên code cũ. **(b) Không xuyên cây:** `NavService.moveAlong` (detour `moveAlongSurface`) + `Bot.slide()` cho **mọi** bước đi thẳng (path/ waypoint/ đội hình/ ló-nấp) → mép navmesh = tường; đệm bán kính obstacle theo loài `NAV_RADIUS_PAD` (tree_gn 1,0 m — tán rễ bạnh; palm 0,3; bamboo 0,15) dùng chung runtime + `terrain-bake --nav` (nav.bin bake lại, 4643 poly). Hệ quả: bot đi vòng mép khối cover thay vì cắt góc (unit ai.test: PEEK_FIRE 5,3 → 6,0 s, ngưỡng nới 6,5 s; thêm guard 0 tick trong khối — code cũ 18 tick). E2E rừng: đồng đội theo 15 s `inTrunk = 0`. Đồng đội chạy 1,8× khi `run` để theo kịp nước rút | `src/engine/render/characters.ts`, `src/engine/nav/navmesh.ts`, `src/game/ai/bot.ts`, `src/engine/vegetation/navObstacles.ts`, `scripts/terrain-bake.mjs`, `public/assets/terrain/truong-son-a/nav.bin` |

