# TIP-009: E2E Playwright + Snapshot G0 + Evidence

## HEADER
- TIP-ID: TIP-009 · Project: HT-MB · Module: `tests/e2e`, `scripts/snapshot.mjs`, `snapshots/G0`, `contracts/`, `README.md`
- Depends on: TIP-008 · Priority: P0 · Estimated effort: 30 min
- REQ: G0-13, G0-14, G0-16 (hướng dẫn bench)

## CONTEXT
- Key files: PRD §11 (lớp E2E: boot, chọn backend, load mission, debug teleport, complete objective), §9 (snapshots/<gate>), ADR-004
- Sandbox: chỉ WebGL2 (SwiftShader). Project `webgpu` chạy trên Mac.

## TASK
E2E smoke chạy được ở CI (WebGL2) và trên Mac (WebGPU), script snapshot G0, contracts YAML cho 9 TIP, README hướng dẫn chạy/bench.

## SPECIFICATIONS
- `tests/e2e/boot.spec.ts`: mở `/?backend=webgl&autostart=1&debug=1` → chờ `window.__ht.ready === true` (≤ 30 s) → `backend === 'webgl2'` → 120 frame không console error (lọc warning WebGPU fallback) → screenshot `evidence/TIP-009/boot-webgl.png`.
- `tests/e2e/mission.spec.ts`: boot → `__ht.teleport(relay_zone)` → chờ subtitle D03 hiện → `__ht.kill('bot_group_a')` → chờ `mission.state().currentNode === 'n_done'` và `MISSION_COMPLETE` count === 1 → `checkpoint.load()` → hash khớp.
- `tests/e2e/bench.spec.ts`: `?bench=1&runs=1&seconds=3` → chờ request `POST /__bench` → file tồn tại và validate schema.
- `tests/e2e/webgpu.spec.ts` (project `webgpu`, skip nếu `!HT_WEBGPU`): boot không `backend` → `backend === 'webgpu'`, adapter info không rỗng, 300 frame không device lost.
- `scripts/snapshot.mjs`: ghi `snapshots/G0/{build-hash.txt (git rev + dist hash sha256), package-lock.json (copy), manifest.json (danh sách file src + size), replay-seed.txt, known-issues.md (từ evidence)}`.
- `contracts/TIP-00x.yaml`: `{id, goal, scope: [paths], acceptance: [...], tests: [...], evidence: [...], rollback, status}` cho 9 TIP (điền từ docs/tips + completion report).
- `README.md`: cài, dev, test, e2e, `npm run bench` trên Mac (Chrome), đọc verdict, cấu trúc repo, vai trò Vibecode.
- CI local: `npm run ci` = typecheck + test + build + e2e (webgl-ci).

## ACCEPTANCE CRITERIA
- Given sandbox, When `npm run ci`, Then exit 0; 3 spec webgl pass; webgpu spec skipped (có lý do).
- Given `npm run snapshot`, Then 5 file trong `snapshots/G0/` và `build-hash.txt` khớp `git rev-parse HEAD`.
- Given `contracts/*.yaml`, Then 9 file, mỗi file có 8 trường; `status` khớp Completion Report.
- Given README, Then có lệnh bench và giải thích verdict PASS/WARN/FAIL theo `performance-budget.json`.

## CONSTRAINTS
- Không hạ tolerance test để pass; fail thật → ghi ISSUES.
- Không đưa video vào git (gitignore).

## REPORT FORMAT
Completion Report → `evidence/TIP-009/completion-report.md` + screenshots.
