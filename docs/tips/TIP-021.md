# TIP-021: Khí tài & phương tiện thật (Sketchfab CC-BY) cho Phố Vạn Hải — xác xe, APC, xe tải, bán tải, xe máy, bao cát, trực thăng

## HEADER
- TIP-ID: TIP-021 · Project: HT-MB · Module: `scripts/convert-model.mjs` (mới), `src/engine/level/{types,barricades,builder}.ts`, `src/engine/render/assets.ts` (`loadModel`), `src/qa/debugApi.ts` (`spawnModel`), `content/levels/pho-van-hai.level.json` (+ `scripts/gen-level-pho.mjs`), `content/assets/manifest.json`, `CREDITS.md`
- Dependencies: TIP-019 (level), D-060 (CC-BY Sketchfab mọi loại), ADR-006 (pipeline Sketchfab + credit), PRD §9.2
- Priority: P0 (D-057 mục 3: "khí tài phương tiện") · Effort: 4 h Thợ

## CONTEXT
- Chủ nhà đăng nhập Sketchfab trong cửa sổ trình duyệt tích hợp; Thợ chọn model, bấm tải `.glb` (bản 1K của Sketchfab), file về `~/Downloads`, stage vào sandbox, convert, đặt vào level. Không nhập mật khẩu, không tạo tài khoản.
- Tiêu chí chọn: CC-BY 4.0, **không phải asset rip từ game** (PRD §9.2: không dùng asset CoD/IP), ≤ 30k tam giác, texture ≤ 2K, kiểu dáng hợp bối cảnh Việt Nam (BTR-70/Ural/Vespa/bán tải Nhật).
- Bị loại sau khi kiểm tra: **BTR-80 (42manako)** — mô tả ghi "Ripped from Squad", tag `ripped`; **helicopter mi-8 (tnikita)** — tên material `bo2_mtl_*`, `bo2_gfx_*` = rip từ Call of Duty: Black Ops 2 → vi phạm trực tiếp §9.2. Hai file này chỉ nằm trong `~/Downloads` của Chủ nhà, không vào repo.

## TASK (đã làm)
1. **`scripts/convert-model.mjs`** (tổng quát hoá `convert-weapon.mjs`): chuẩn hoá trục dài nhất → +x, đáy y = 0, tâm xz = 0, scale theo `--length`/`--scale`; `--keep/--drop` node; **nướng skin** (xe buýt Sketchfab có skin tầm thường → vị trí thật, bỏ JOINTS/WEIGHTS); `--merge-mats` gộp material trùng tên (Door_0..4); flatten + join primitive cùng material (draw = số material); `--no-join` giữ pivot (cánh quạt trực thăng); texture WebP ≤ 1K; quantize + meshopt; ghi manifest (`source: sketchfab`, `license: CC-BY-4.0`, `attribution`, `size`).
2. **Asset** (11 model, 9,1 MB sau convert; nguồn 58 MB trong `assets-src/sketchfab/`, gitignore):

   | id | Model (tác giả) | tri | Dùng |
   |---|---|---|---|
   | `veh_btr70` | Low-poly BTR-70 (veightyfive) | 10k | xác APC cháy chốt A (tint 0,45 + lửa/khói) |
   | `veh_wreck_bus` | Abandoned Wrecked Bus (BytesCrafter) | 12k | xe buýt chắn ngã tư C |
   | `veh_wreck_car_a/b` | Burned-out Cars (Renafox) | 2k + 1,5k | 3 xác xe hơi cháy (B, C, hẻm) |
   | `veh_pickup` | Lightbody '90 MD Utility (Daniel Zhabotinsky) | 25k | bán tải bỏ giữa A–B (cover); technical TIP-023 |
   | `veh_ural` | Ural 4320 (Brout) | 8,5k | xe tải quân sự đỗ trước trạm C |
   | `veh_scooter` | Old Scooter — Vespa (Nadia Ribitis) | 3,5k | 7 xe dựng vỉa hè + 1 ngã giữa đường (`roll`) |
   | `veh_mi24` | Mi-24 Hind (Duane's Mind) | 4,2k | trực thăng bay ngang (TIP-023; node rotor giữ pivot) |
   | `prop_sandbag_02/05` | Sandbag Wall 02/05 (Pypunk) | 1,2k / 0,8k | tường bao cát chốt A/C (scale 1,4 → cao 1,0 m) |

3. **Level format**: `BarricadeDef.model/size/scale/tint` — có model + `size` → builder instance model thật, collider/cover lấy từ `size` (xe: 4 mặt cover; bao cát: lặp module theo `length`); thiếu model (`?assets=0`) → procedural cũ. `PropDef.roll/pitch/tint`. Builder: `instanceModel()` chung cho prop + barricade (InstancedMesh theo primitive, `instanceColor` cho ám khói).
4. **Ngân sách payload**: bỏ `modular_electricity_poles` (6 MB, không dùng từ TIP-019 — cột điện procedural); xe cháy texture 512 → manifest 145 MB ≤ 150 MB. Không sửa budget.
5. Debug: `__ht.spawnModel(id, x, y, z, yaw, scale)` để lineup/kiểm tra model trong level (`scratchpad/lineup21.mjs`).

## ACCEPTANCE CRITERIA
- Given `npm run dev`, Then chốt A có BTR-70 cháy, ngã tư C có xe buýt + xe hơi cháy, Ural trước trạm, bán tải giữa phố, xe máy dọc vỉa hè; collider khớp hình (đi vòng, nấp được).
- Given `?assets=0`, Then level vẫn dựng (procedural), không lỗi.
- Given manifest, Then mọi model CC-BY có attribution + url + authors; CREDITS.md và màn capability hiển thị; tổng ≤ 150 MB.
- Given unit test, Then 102/102; level.test kiểm tra model của barricade/prop có trong `models`.

## KẾT QUẢ
- Xem `evidence/TIP-021/completion-report.md`.
