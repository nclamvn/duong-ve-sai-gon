# TIP-001: Scaffold + Governance

## HEADER
- TIP-ID: TIP-001 · Project: HT-MB · Module: root, docs, config, content/schemas
- Depends on: None · Priority: P0 · Estimated effort: 25 min
- REQ: G0-01, G0-14 (khung), G0-15 (i18n khung)

## CONTEXT
- Working directory: `hai-tuyen/`
- Key files to reference: `docs/PRD.md` §3, §4.1, §8, §9; `docs/BLUEPRINT.md` (tech stack pinned, file structure)
- Patterns: ESM, TS strict + `noUncheckedIndexedAccess`; không default export trừ config

## TASK
Dựng repo chạy được `npm run dev / build / typecheck / test / e2e` với dependency pin đúng Blueprint, kèm bộ artifact governance PRD §9 (AGENTS.md, ADR-001..004, performance-budget.json, schemas khung, locale vi.json).

## SPECIFICATIONS
1. `package.json`: dependencies pin chính xác (không `^`): three 0.185.1, @dimforge/rapier3d-compat 0.20.0, recast-navigation 0.43.1, @recast-navigation/three 0.43.1, ajv 8.20.0. Dev: vite 8.2.2, typescript 7.0.2, vitest 4.1.11, @playwright/test 1.62.1, @types/three (tương thích r185).
2. Scripts: `dev`, `build`, `preview`, `typecheck` (tsc --noEmit), `test` (vitest run), `e2e` (playwright test), `e2e:webgpu` (project webgpu, headed), `bench` (vite với `--mode bench` + mở trình duyệt), `snapshot` (scripts/snapshot.mjs).
3. `tsconfig.json`: strict, target ES2022, moduleResolution bundler, paths `@engine/*`, `@game/*`, `@ui/*`, `@qa/*`, `@content/*`.
4. `vite.config.ts`: alias, `optimizeDeps.exclude` cho rapier/recast (WASM), plugin `benchPlugin()` từ `scripts/bench-plugin.ts` (stub trong TIP này, hoàn thiện TIP-004), `define __BUILD_HASH__` từ git rev-parse (fallback `dev`).
5. `playwright.config.ts`: project `webgl-ci` (chromium headless, `executablePath` từ env `HT_CHROME` nếu có, URL `?backend=webgl`), project `webgpu` (channel chrome, headed, chỉ chạy khi `HT_WEBGPU=1`). `webServer` = `vite preview --port 4173` sau build.
6. `config/performance-budget.json`: toàn bộ bảng PRD §4.1 (target/red/method) + `evidence_status: "engineering_default_requires_review"`, `device: "MacBook Pro M1 Max 32GB"`, `resolution_default: [1920,1200]`.
7. `content/schemas/*.schema.json` (draft-07): mission, dialogue, checkpoint, performance-replay theo trường bắt buộc PRD §8. `content/locale/vi.json` với key cho capability screen, HUD, 3 cue radio G0.
8. `AGENTS.md`: luật kiến trúc (bảng "không được làm" §3.2), lệnh test, vùng cấm (`engine/render` không import `game/*`; `content/` không chứa .ts), convention completion report, escalation L1/L2/L3.
9. `docs/ADR/ADR-001-three-version.md`, `ADR-002-rapier-compat.md`, `ADR-003-recast-runtime-navmesh.md`, `ADR-004-ci-webgl-fallback.md` (context / decision / consequence).
10. `.gitignore` (node_modules, dist, test-results, playwright-report, evidence/**/video).
11. `src/main.ts` tối thiểu: render "HT-MB boot" vào DOM để `build` pass; sẽ thay ở TIP-003.

## ACCEPTANCE CRITERIA
- Given repo sạch, When `npm install && npm run typecheck && npm run build`, Then exit 0, không warning về peer dep của three.
- Given `npm run test`, When chưa có test, Then vitest pass với 1 smoke test `tests/unit/budget.test.ts` kiểm `performance-budget.json` có 14 chỉ số và `evidence_status` đúng.
- Given `content/schemas/mission.schema.json`, When validate `{}` bằng ajv, Then fail vì thiếu `id, version, seedPolicy, startNode, checkpoints, localization`.
- Given `AGENTS.md`, Then chứa 5 mục: Kiến trúc, Lệnh, Vùng cấm, Completion Report, Escalation.

## CONSTRAINTS
- Không cài package ngoài danh sách (trừ `@types/three`, `@types/node`).
- Không viết logic game trong TIP này.
- Không tải asset từ mạng.

## REPORT FORMAT
Completion Report chuẩn → `evidence/TIP-001/completion-report.md`.
